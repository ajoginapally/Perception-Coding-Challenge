We tracked a stationary traffic light in the images, used its depth measurements to locate it relative to the camera, and reversed that relationship to estimate the car’s position relative to the light. We plotted those positions from above and animated them over time, leaving gaps where depth was missing; the current estimate assumes constant heading, so vehicle turns can distort the trajectory.